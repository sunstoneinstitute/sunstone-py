name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

    - name: Install uv
      uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

    - name: Set up Python
      run: uv python install 3.12

    - name: Build package
      run: uv build

    - name: Upload build artifacts
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
      with:
        name: dist
        path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
      with:
        name: dist
        path: dist/

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b # release/v1

  docs:
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      with:
        fetch-depth: 0

    - name: Install uv
      uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

    - name: Set up Python
      run: uv python install 3.12

    - name: Install docs dependencies
      run: uv sync --group docs

    - name: Configure git
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

    - name: Deploy docs
      run: |
        VERSION="${GITHUB_REF#refs/tags/v}"
        uv run mike deploy --push --update-aliases "$VERSION" latest
        uv run mike set-default --push latest

  notify:
    needs: publish
    runs-on: ubuntu-latest
    environment: pypi
    steps:
    - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

    - name: Install uv
      uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

    - name: Send Google Chat notification
      run: |
        uv sync
        url="${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}"
        version="${GITHUB_REF#refs/tags/v}"
        message="New release of sunstone-py available: $version"
        changelog=$(sed -ne "/## \[$version\]/,/## \[/p" CHANGELOG.md | sed '1d;$d' | uv run markdown_py)
        pypi_url="https://pypi.org/project/sunstone-py/${version}/"
        docs_url="https://sunstoneinstitute.github.io/sunstone-py/${version}/"
        jq -n --arg message "$message" --arg changelog "$changelog" --arg pypi_url "$pypi_url" --arg docs_url "$docs_url" '{
          text: $message,
          cards: [{
            sections: [{
              widgets: [{
                textParagraph: {
                  text: "<b>Changelog</b><br>\($changelog)"
                }
              },
              {
                textParagraph: {
                  text: "<b>Links</b><br><a href=\"\($pypi_url)\">PyPI</a> · <a href=\"\($docs_url)\">Documentation</a>"
                }
              }]
            }]
          }]
        }' | curl -X POST -H 'Content-Type: application/json' -d @- "$url"

  github-release:
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

    - name: Extract version from tag
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_ENV"

    - name: Extract release notes from CHANGELOG.md
      run: |
        {
          echo "CHANGELOG<<__EOF__"
          sed -ne "/## \[$VERSION\]/,/## \[/p" CHANGELOG.md | sed '1d;$d'
          echo "__EOF__"
        } >> "$GITHUB_ENV"

    - name: Extract installation section from README.md
      run: |
        {
          echo "INSTALLATION<<__EOF__"
          sed -ne "/## Installation/,/## /p" README.md | sed '$d'
          echo "__EOF__"
        } >> "$GITHUB_ENV"

    - name: Create GitHub Release
      run: |
        gh release create "v$VERSION" \
          --title "v$VERSION" \
          --notes "<details>
        <summary>Changelog for v$VERSION</summary>

        $CHANGELOG

        </details>

        See the <a href='https://sunstone-data.github.io/sunstone-py/$VERSION/'>documentation</a> for detailed docs.

        $INSTALLATION"
      env:
        GH_TOKEN: ${{ github.token }}
      continue-on-error: true
